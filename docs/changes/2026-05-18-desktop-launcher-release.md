# 2026-05-18 Desktop Launcher Release

- bumped the skill version to `0.02`
- added a shipped `dashboard wslg.desktop` launcher for the GNOME desktop session
- switched install examples to `dashboard skills install wslg`
- fixed the release metadata so remote `dashboard skills install wslg` refreshes to the new skill contents
