# 2026-05-18 Initial Release

- added the initial `wslg` skill
- added `dashboard wslg.setup` with Ubuntu `20.04`, `22.04`, and `24.04` support
- added `dashboard wslg.desktop` to start the Ubuntu GNOME desktop session with the required environment
- added a `--dry-run` review path for generated WSLg commands and file contents
- documented the WSLg prerequisite, the GNOME follow-up start command, the short install form `dashboard skills install wslg`, and the release-specific override behavior
