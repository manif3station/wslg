0.09 2026-05-18
 - make `wslg.setup` run `systemctl daemon-reload` and restart `wslg-fix.service` immediately after writing the unit
 - give `wslg.desktop` a sane fallback `XDG_DATA_DIRS` when the inherited environment is empty

0.08 2026-05-18
 - change the desktop dry-run preview to copy-paste-safe `export ...` lines
 - launch the desktop with `XDG_SESSION_DESKTOP=ubuntu` and `gnome-session --session=ubuntu`

0.07 2026-05-18
 - make `dashboard wslg.desktop` launch GNOME without printing the desktop summary first
 - keep the human-readable desktop summary for `dashboard wslg.desktop --dry-run`

0.06 2026-05-18
 - add `dashboard wslg.desktop --size WIDTHxHEIGHT`
 - wire the requested size into `MUTTER_DEBUG_DUMMY_MODE_SPECS`

0.05 2026-05-18
 - run `dashboard wslg.desktop` through a direct `env ... gnome-session` argv path instead of `/bin/sh -lc`
 - avoid shell parsing errors from the multiline environment block on real hosts

0.04 2026-05-18
 - change `dashboard wslg.setup` and `dashboard wslg.desktop` summaries from JSON to readable text
 - keep the same internal result structure while rendering a CLI-friendly summary

0.03 2026-05-18
 - fix `dashboard wslg.setup` so it writes `wslg-fix.service` before enabling it
 - prove the real failure on `claudev1` and align the setup order with that host result

0.02 2026-05-18
 - add `dashboard wslg.desktop` to start the Ubuntu GNOME desktop session
 - shorten install examples to `dashboard skills install wslg`
 - cut a versioned refresh so remote `dashboard skills install wslg` updates correctly

0.01 2026-05-18
 - package the initial `wslg` skill
 - add `dashboard wslg.setup` for Ubuntu WSLg GNOME bootstrap
 - add Docker-tested docs, ticket records, and MIT license coverage
