# EPIC-205

## Title

Add a governed `wslg` setup skill for Ubuntu WSLg desktop bootstrap.

## Goal

Package the referenced Ubuntu WSLg GNOME setup flow into a reusable Developer Dashboard skill so the user can run `dashboard wslg.setup` instead of manually replaying release-specific systemd edits and package commands.

## Status

Done
