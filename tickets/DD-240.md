# DD-240

## Title

Build `dashboard wslg.setup` for Ubuntu WSLg GNOME bootstrap.

## Method

TDD

## Acceptance

- `dashboard wslg.setup` supports Ubuntu `20.04`, `22.04`, and `24.04`
- the setup flow validates that it is running inside WSL and on Ubuntu before applying changes
- the setup flow installs the documented package set, masks `gdm.service`, writes the `wslg-fix.service` unit, and writes the correct GNOME nested override path for the chosen Ubuntu version
- `dashboard wslg.setup --dry-run` returns the generated plan without applying system changes
- Docker tests and `100%` coverage pass
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push are completed

## Status

Done
